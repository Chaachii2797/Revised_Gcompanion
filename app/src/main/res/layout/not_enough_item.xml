<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="250dp"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/dialog_img_notEnough"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="15dp"
        android:src="@drawable/done"
        app:srcCompat="@drawable/not_enough" />

    <TextView
        android:id="@+id/tv_notEnough"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/dialog_img_notEnough"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="5dp"
        android:gravity="center"
        android:textColor="@color/gen_scheme4"
        android:textSize="20sp"
        android:textStyle="bold"
        tools:text="Not Enough Quantity" />

    <Button
        android:id="@+id/dialog_btn_ok"
        style="@style/LoginStyle"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tv_notEnough"
        android:layout_alignStart="@+id/tv_notEnough"
        android:layout_below="@+id/dialog_img_notEnough"
        android:layout_marginBottom="15dp"
        android:layout_marginTop="28dp"
        android:backgroundTint="@color/gen_scheme4"
        android:gravity="center"
        android:text=" Ok " />

</RelativeLayout>